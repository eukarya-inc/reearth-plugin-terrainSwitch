id: terrain-switch-plugin
name: terrain switch plugin
version: 1.0.0
extensions:
  - id: terrain-switch
    type: widget
    name: terrain toggle
    schema:
      groups:
        - id: ui
          title: ui
          fields:
            - id: color
              title: color
              type: string
              ui: color
